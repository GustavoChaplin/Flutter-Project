import 'package:flutter/material.dart';

class Defaultstyle extends StatelessWidget {
  const Defaultstyle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}